<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body, html {
      height: 100%;
      font-family: 'Poppins', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
    
    }

    /* Full-screen video background */
    .bg-video {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      object-fit: cover;
      z-index: -1; /* วิดีโออยู่ข้างหลังเนื้อหา */
    }

    .form-container {
      position: relative;
      z-index: 1; /* วางฟอร์มให้อยู่ข้างหน้า */
      background-color: rgba(255, 255, 255, 0.8); /* ฟอร์มโปร่งแสงเล็กน้อย */
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
      text-align: center;
      width: 300px;
    }

    h3 {
      margin-bottom: 20px;
      color: #333;
    }

    label {
      display: block;
      margin-bottom: 10px;
      text-align: left;
      font-size: 14px;
      color: #333;
    }

    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      width: 100%;
      padding: 10px;
      background-color: #ff6347;
      border: none;
      border-radius: 5px;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #ff4500;
    }

    .register-link {
      margin-top: 15px;
      font-size: 14px;
    }

    .register-link a {
      color: #ff6347;
      text-decoration: none;
    }

    .register-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <!-- Video Background -->
  <video autoplay loop muted playsinline class="bg-video">
    <source src="https://cdn.pixabay.com/video/2024/07/28/223580_large.mp4" type="video/mp4">
    <source src="https://cdn.pixabay.com/video/2024/07/28/223580_large.webm" type="video/webm">
    Your browser does not support the video tag.
  </video>

  <!-- Form Container -->
  <div class="form-container">
    <h3>Login Here</h3>

    <form action="/login" method="post">
      <label for="username">Username</label>
      <input type="text" name="username" id="username" placeholder="Email or Phone" required>

      <label for="password">Password</label>
      <input type="password" name="password" id="password" placeholder="Password" required>

      <button type="submit">Log In</button>
    </form>

    <div class="register-link">
      Don't have an account? <a href="/register">Register Here</a>
    </div>
  </div>

</body>
</html>
